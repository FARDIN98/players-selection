import React from 'react';

const Cart = (team) => {
    // const cricketer = props
    console.log(team)
    return (
        <div>
            {/* <h1>This is cart: {props.team}</h1> */}
        </div>
    );
};

export default Cart;